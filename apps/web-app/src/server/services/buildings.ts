import { type PrismaClient } from "@prisma/client";
import { type CreateBuildingInput } from "~/lib/building";


export const createOrUpdateBuildings = async (
  input: CreateBuildingInput,
  userId: string,
  db: PrismaClient,
) => {

    
  
};
